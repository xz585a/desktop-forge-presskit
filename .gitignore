# 配布用の大きなファイルは Git で管理しない。
# 差し替えるたびに履歴へ丸ごと積み上がり、二度と減らないため。
# 実体は GitHub Releases の `assets` タグに置く（README「配布アセットの置き場」を参照）。

downloads/*.zip
assets/video/*.mp4

# 手元の作業ファイル。公開する確定版だけをコミットする。
*_wip.*
*_tmp.*
*_old.*
